{%- from "elasticsearch/map.jinja" import elasticsearch with context -%}
{%- if elasticsearch.shield['roles'] is defined %}
{% set shield_roles = elasticsearch.shield['roles'] %}
{{ shield_roles|yaml(False) }}
{%- else %}
# no shield role options set
{%- endif %}
